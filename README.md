# my-react-package

## Setup

```bash
git clone REPO

cd REPO

yarn
```

## Examples

### Typescript Build

Compile the current project and will generate type definitions. 
You can see the output in `lib/`

```
yarn tsc -d
```
